version: "3.9"
services:
  web:
    image: devsecops-demo:${IMAGE_TAG:-latest}
    ports:
      - "8000:8000"
    environment:
      - DEMO_API_KEY=${DEMO_API_KEY:-local_demo}
    restart: unless-stopped

